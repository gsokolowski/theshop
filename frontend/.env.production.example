# Copy to .env.production on the server before: npm run build
# Filename must be .env.production (gitignored if you keep secrets out of repo).

VITE_API_BASE_URL=https://the-shop.codecreators.co.uk/api/v1
